import { createClient } from '@supabase/supabase-js'

// This is just a test file to verify the import works
const testImport = () => {
  console.log('Supabase import is working!')
  return true
}

export default testImport 